import React from "react";

export default function Movie(key, id, title,) {
  return (
    <div className="container">
      <h1>{title}</h1>
      {/* <img src={image} alt="" />
      <h3>{ImdbRating}</h3> */}
    </div>
  );
}
